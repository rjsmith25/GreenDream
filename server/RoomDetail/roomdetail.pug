//- RoomDetail
extends ../Components/layout

block content
  <div id="modal"></div>
  <header class="general-header">!{GeneralHeaderContent}</header>
  <section class="room-detail">!{RoomDetailContent}</section>
block scripts
  script(id='page-ssr').
    window.__data__ = !{JSON.stringify(roomData).replace(/<\//g, "<\\/")}
  script(src='https://js.stripe.com/v2/', type='text/javascript')
  script(src='/js/room-detail.bundle.js')
  
  
