//- RoomDetail
extends ../Components/layout
include ../Components/mixin/general-header

block content
  +generalHeader(roomtype)
  <section class="room-detail">!{content}</section>
block scripts
  script(id='page-ssr').
    window.__data__ = !{JSON.stringify(roomData).replace(/<\//g, "<\\/")}
  script(src='/js/room-detail.bundle.js')
  
  
