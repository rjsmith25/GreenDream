//- About Us
extends ../Components/layout

block content
  <header class="general-header">!{GeneralHeaderContent}</header>
  include ./includes/about-us
  include ./includes/about-team
block scripts
  script(id='page-ssr').
    window.__data__ = !{JSON.stringify(aboutData).replace(/<\//g, "<\\/")}
  script(src='/js/about.bundle.js')
      
