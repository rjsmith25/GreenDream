//- RoomsPage
extends ../Components/layout

block content
  <header class="general-header">!{GeneralHeaderContent}</header>
  <section class="rooms">!{RoomsContent}</section>
block scripts
  script(id='page-ssr').
    window.__data__ = !{JSON.stringify(roomsData).replace(/<\//g, "<\\/")}
  script(src='/js/rooms.bundle.js')
